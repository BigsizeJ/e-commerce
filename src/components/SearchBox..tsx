import styled from "styled-components";

const SearchBox = () => {
  return (
    <SearchCtn>
      <label></label>
      <input type={"search"} />
    </SearchCtn>
  );
};

const SearchCtn = styled.div``;

export default SearchBox;
