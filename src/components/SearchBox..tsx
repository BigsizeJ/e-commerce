import styled from "styled-components";

const SearchBox = () => {
  return (
    <SearchCtn>
      <input type={"search"} />
    </SearchCtn>
  );
};

const SearchCtn = styled.div``;

export default SearchBox;
